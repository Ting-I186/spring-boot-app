name: "capitec-spring-boot-app"
enabled: true
person:
    name: "Ting-I"
    surname: "Liang"
spring:
    application:
        name: config-client
    profiles:
        active: development
    config:
        import: optional:configserver:http://localhost:31901
    cloud:
        config:
            label: main
            username: root
            password: s3cr3t
